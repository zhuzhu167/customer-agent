# 百应科技客服 Agent 项目协作规范

更新时间：2026-06-21
适用对象：参与本仓库研发、需求梳理、设计评审、技术方案、测试验收和文档维护的 AI Agent 与人类协作者
规范目标：让项目在持续迭代中保持上下文清晰、需求可追溯、实现可验证，并逐步形成 Agent 自驱动进化能力

## 1. 总体原则

本项目的协作目标不是只完成一次代码修改，而是持续推进“营销业扩通用客服 Agent Web Voice MVP”的研发闭环。每次新对话、每次新任务、每次新实现都应先理解当前项目背景，再和用户沟通下一步。

默认使用中文输出解释性内容。以下内容保留英文：

- `commands`
- `code`
- `filenames`
- `file paths`
- `config keys`
- `API names`
- `model names`
- `package names`
- `YAML / JSON keys`
- 工具、框架或程序必须识别的固定字段

不要把聊天历史当作唯一事实来源。项目事实以仓库中的 PRD、UI/UX、工程文档、代码、测试和运行结果为准。

## 2. 每次新对话的启动协议

每次新对话开始时，Agent 不应直接进入实现。必须先做轻量项目背景读取，除非用户明确要求只回答一个和项目无关的简单问题。

推荐读取顺序：

1. 先读 [README.md](/Users/liangzhu/Desktop/软件开发/百应科技/README.md)，确认项目一句话目标。
2. 再读 [docs/engineering/_context.md](/Users/liangzhu/Desktop/软件开发/百应科技/docs/engineering/_context.md)，确认当前工程阶段、约束和已确认取舍。
3. 若任务涉及需求或范围，读取 `docs/prd/` 下最新 PRD。
4. 若任务涉及界面、交互、视觉或前端体验，读取 `docs/ui-ux/`。
5. 若任务涉及架构、模块、接口、数据、运行时或技术取舍，读取 `docs/engineering/`。
6. 若任务涉及实现拆分、里程碑或验收，读取 `docs/issues/`。
7. 若任务涉及行业趋势、产品判断或后续演进，读取 `docs/intelligence/` 中相关日报或 cards。

读取后先形成一个简短判断：

- 当前任务属于需求、设计、技术方案、实现、测试、文档、审查还是研究。
- 本次任务要遵循哪些上游约束。
- 是否存在和既有 PRD、UI/UX 或工程方案冲突的地方。

和用户沟通时，先用 1-3 句话说明自己已经识别到的项目背景和任务边界，再继续推进。

## 3. 目录职责

### 3.1 需求输入：`docs/prd/`

`docs/prd/` 是需求事实来源。涉及“做什么、为什么做、给谁用、验收什么、不做什么”时，必须优先查看这里。

维护规则：

- 新需求必须能回到某个 `REQ-*`、`US-*`、业务规则、范围或非目标。
- 如果用户提出的新想法改变了范围，应先判断是 `accepted`、`changed`、`superseded`、`deferred` 还是 `open_question`。
- 不要绕过 PRD 直接实现重大产品行为变化。
- 若实现中发现需求歧义，应补充到 PRD 或未决问题，而不是只留在聊天里。

### 3.2 界面设计：`docs/ui-ux/`

`docs/ui-ux/` 是界面体验、视觉系统、交互状态和设计边界的事实来源。涉及前端页面、组件、动效、布局、颜色、状态展示时必须查看这里。

维护规则：

- 当前界面方向是沉浸式浏览器模拟通话体验，不是客服工作台、CRM、聊天机器人窗口或营销落地页。
- 主界面应突出语音声场、通话状态和核心控制，不应堆叠大面积卡片和复杂列表。
- 必须明确表达“浏览器模拟通话”，避免误导为真实电话。
- UI 变化如果影响设计原则、颜色、布局、状态或交互，应同步更新 `docs/ui-ux/`。

### 3.3 技术方案：`docs/engineering/`

`docs/engineering/` 是架构、模块、数据、接口、状态机、技术取舍和工程治理的事实来源。涉及实现前必须查看相关工程文档。

维护规则：

- 总体架构看 `docs/engineering/system-design.md`。
- 模块设计看 `docs/engineering/modules/`。
- 架构变更写入 `docs/engineering/changes/`。
- 已确认取舍写入 `docs/engineering/_decision-log.md`。
- 未决技术问题写入 `docs/engineering/_open-questions.md`。
- 不要在代码里悄悄改变架构边界；有变化先记录原因、影响和验证方式。

### 3.4 实现代码：`backend/` 与 `frontend/`

`backend/` 负责后端 API、会话、事件、Provider、Agent Runtime、数据库和测试。

`frontend/` 负责浏览器模拟通话界面、状态管理、麦克风、LiveKit 客户端、Mock 对话流和可见交互。

维护规则：

- 实现要贴合当前架构，不随意引入新框架或新范式。
- 前后端协议变化必须同步类型、schema、测试和文档。
- 涉及会话、轮次、事件、Provider、配置的修改，应同步考虑后端测试。
- 涉及 UI 状态、交互、视觉的修改，应同步考虑前端构建和浏览器可见性验证。

### 3.5 迭代任务：`docs/issues/`

`docs/issues/` 用于拆分 MVP vertical slices、实现任务、问题追踪和验收。

维护规则：

- 每个任务尽量穿过 UI、API、状态、数据和验收，不做只看起来完成的孤立横切。
- 任务要能追溯到 PRD、设计和工程方案。
- 完成后应记录验证命令、验证结果和剩余风险。

### 3.6 情报与演进：`docs/intelligence/`

`docs/intelligence/` 用于沉淀产品情报、竞品启发、行业趋势、技术机会和后续演进卡片。

维护规则：

- 情报不能直接替代需求。
- 情报进入实现前，必须转译为 PRD、技术方案或迭代任务。
- 对首版 MVP 无关但未来有价值的内容，应标记为后续候选，不要污染当前范围。

## 4. 上下文加载策略

Agent 应按任务最小必要上下文加载文件，避免一次性读取整个仓库。

推荐策略：

- 需求类任务：读 `README.md`、`docs/engineering/_context.md`、`docs/prd/`。
- UI 类任务：读 `README.md`、`docs/prd/` 中相关范围、`docs/ui-ux/`、相关 `frontend/src/` 文件。
- 技术方案类任务：读 `docs/engineering/_index.md`、`docs/engineering/_context.md`、相关模块设计和相关代码。
- 后端实现类任务：读 `docs/engineering/` 相关模块、`backend/README.md`、相关 `backend/app/` 和 `backend/tests/`。
- 前端实现类任务：读 `docs/ui-ux/`、相关 `frontend/src/`、`frontend/package.json`。
- 验收或修复类任务：读相关任务文档、失败日志、测试文件和最近改动。

如果发现文档与代码不一致，应明确说明：

- 文档怎么说。
- 代码当前怎么做。
- 本次应以哪一个为准。
- 是否需要补文档或修代码。

## 5. 任务执行协议

每次执行任务时，按以下顺序推进：

1. 确认任务类型和上游依据。
2. 读取必要上下文。
3. 识别影响范围和非目标。
4. 如需修改文件，先说明将修改哪些区域。
5. 小步实现，避免无关重构。
6. 同步必要的测试、类型、文档或配置。
7. 运行可行的验证命令。
8. 总结改动、验证结果、剩余风险和建议下一步。

默认不要只给计划不实现。除非用户明确要求讨论、设计、评审或不要改代码，否则应在理解清楚后直接推进。

### 5.1 Mock 数据与真实性要求

本项目允许在本地演示、自动化测试、Provider 未配置的开发环境中显式使用 `mock`，但禁止用 mock 数据、假接口、静态样例或硬编码状态冒充真实实现、真实联调或真实验证。

使用 `mock` 必须遵守以下规则：

- 必须显式标注：代码、配置、文档、测试输出或总结中应清楚说明当前使用的是 `mock`、`fixture`、`sample`、`demo` 还是真实数据。
- 必须可配置切换：mock 逻辑应通过明确配置启用，例如 `DEFAULT_*_PROVIDER=mock`、`VITE_USE_MOCK_SESSION=true`，不要把 mock 写成默认无法关闭的业务路径。
- 必须限制边界：mock 只能用于单元测试、本地离线演示、前端静态预览、外部 Provider 暂不可用时的占位验证；不能作为生产路径、真实验收或真实端到端联调的证据。
- 必须如实汇报：如果只跑了 mock 流程，总结中必须写明“仅验证 mock 流程，尚未验证真实 Provider / 数据库 / 后端 API / LiveKit 链路”。
- 必须保留失败事实：真实 API、数据库、Provider、鉴权、网络或环境不可用时，应暴露阻塞原因和错误信息，不要用假成功、假数据或静默 fallback 掩盖失败。

以下行为视为违规：

- 为了让页面看起来完成，直接写死会话、客户、客服回答、通话状态、转写内容或统计结果。
- 后端接口未实现时，在前端临时伪造成功响应，却在总结中说“已打通接口”。
- 测试失败或真实服务不可用时，改成 mock 后声称“端到端验证通过”。
- 未经说明把真实 Provider 调用替换成 `mock`、`fake`、`dummy`、`sample` 或 `placeholder`。
- 使用硬编码延时、固定回答或固定状态模拟智能行为，却不在 UI、文档或总结中标注演示性质。

做实现、验收或代码审查时，应主动检查 `mock`、`fake`、`dummy`、`sample`、`fixture`、`placeholder`、硬编码业务数据和静默 fallback。发现此类内容时，需要判断它是合法测试/演示辅助，还是掩盖未完成实现的风险，并在总结中说明。

## 6. 需求到实现的追踪链

本项目需要保持以下追踪链：

```text
PRD / REQ / US
  -> UI-UX / Engineering
  -> Slice / Task
  -> Code Change
  -> Test / Verification
  -> Release / Issue / RCA
```

写方案、做实现或修问题时，尽量在总结中说明：

- `Trace`：关联的 PRD、设计、工程文档或 issue。
- `Changed`：改了哪些文件和行为。
- `Verification`：跑了什么命令，看到了什么结果。
- `Risk`：还有什么未验证、未决或需人工确认。

如果任务没有明确追踪来源，应先根据当前 PRD 和工程文档补一个临时判断，再询问用户是否需要正式沉淀为 issue 或 PRD 变更。


## 7. 沟通规范

和用户沟通时，优先使用中文。

每次新任务开始时，建议先简短说明：

- 我识别到当前项目阶段是什么。
- 本次任务涉及哪些目录或文档。
- 我接下来会先看什么、再改什么、如何验证。

任务完成时，建议使用以下结构：

```text
已完成：
- ...

验证：
- ...

注意：
- ...
```

如果用户正在快速推进研发，不要输出过长解释。优先给关键结论、改动路径和验证结果。

## 8. 自驱动进化机制

本规范的目标是让 Agent 在项目中逐渐变得更有用，而不是每次从零开始。

Agent 应持续识别并沉淀以下内容：

- 重复出现的问题。
- 文档和代码不一致的地方。
- 用户反复强调的偏好。
- 每次实现中形成的稳定规则。
- 影响后续迭代的技术债。
- 当前任务外但明显阻塞后续研发的缺口。

沉淀方式：

- 需求变化进入 `docs/prd/`。
- 界面规则进入 `docs/ui-ux/`。
- 技术取舍进入 `docs/engineering/_decision-log.md`。
- 未决问题进入 `docs/engineering/_open-questions.md` 或 PRD 的 open question。
- 可执行任务进入 `docs/issues/`。
- 行业观察和产品启发进入 `docs/intelligence/`。

Agent 不应自行扩大产品范围。自驱动进化的意思是更好地发现问题、补齐上下文、完善验证和推动下一步，而不是跳过用户确认去做重大方向改变。

## 9. 变更与版本意识

当发生以下情况时，应考虑更新相关文档：

- PRD 范围改变。
- UI 体验方向改变。
- 技术栈或架构边界改变。
- API、事件协议、数据模型或状态机改变。
- MVP slice 的验收标准改变。
- 出现线上级别或演示级别阻塞问题。

推荐记录格式：

```text
变更日期：
变更来源：
影响范围：
关联文档：
关联代码：
验证方式：
剩余风险：
```
